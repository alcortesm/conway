package main

import "fmt"

const (
	ticks  = 15
	width  = 10
	height = 10
)

func main() {
	fmt.Println("TODO")
	/*
		var u Universe
		var a Animator

		for i := 0; i < ticks; i++ {
			s := u.pic()
			a.add(s)
			u.tick()
		}

		a.animate("conway.gif")
	*/
}
